[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn webhook_server:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "always"

[nixpacks]
pythonVersion = "3.11"

